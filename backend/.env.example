# CodeVerse backend – copy to .env and fill in your values
# DO NOT COMMIT .env TO VERSION CONTROL

NODE_ENV=production

# Required for real AI. Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# PostgreSQL Database URL from Render
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/database

# OAuth – required for Google and GitHub sign-in
# Get from Google Cloud Console and GitHub OAuth App
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# JWT secret for access tokens (must be at least 32 characters in production)
JWT_SECRET=your_jwt_secret_here_minimum_32_characters_long

# Port (Render sets PORT automatically)
PORT=3000
CORS_ORIGINS=https://expo.dev
