# CodeVerse backend – copy to .env (do not commit .env)

# Production: set to production (required for strict security and env validation)
# NODE_ENV=production

# Required for real AI. Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: model name (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# OAuth – required for Google and GitHub sign-in
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# JWT: in production must be set and at least 32 characters (e.g. openssl rand -hex 32)
JWT_SECRET=codeverse-dev-secret-change-in-production
# JWT_EXPIRES_IN=7d

# CORS: comma-separated allowed origins (e.g. https://yourapp.com,https://expo.dev). Empty = allow all.
# CORS_ORIGINS=

# Port (Render sets PORT automatically)
PORT=3000
