# CodeVerse – production build env (optional)
# Copy to .env.production only for LOCAL production builds. Prefer EAS Environment for cloud builds.
# Do not commit .env.production. It is in .gitignore.

# Production backend (no trailing slash)
EXPO_PUBLIC_API_URL=https://your-backend.onrender.com

# OAuth – same Client IDs as in backend and Google/GitHub Console
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GITHUB_CLIENT_ID=your-github-client-id

# Optional: redirect URI for production (app uses codeverse-ai:// by default)
# EXPO_PUBLIC_GOOGLE_REDIRECT_URI=codeverse-ai://
