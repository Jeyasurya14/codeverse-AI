# CodeVerse – copy this file to .env and fill in your values.
# Do not commit .env to version control.

# Backend API base URL (no trailing slash).
# Used for AI chat and future auth/articles/payments.
# Leave unset or use the default to run with in-app mock AI.
EXPO_PUBLIC_API_URL=https://api.codeverse.app
